#include <bits/stdc++.h>
using namespace std;

#define INF 100000000000000000
#define INF32 1000000000
#define pii pair<int,int>
#define pll pair<long long int,long long int>
#define ll long long int
#define forn(i,n) for (int i=0;i<n;i++)

/*
	problem description
	in:
    t
    {
        n, l, r
    }

    calculate the number of sequences A such that |A| = n, A is bounded within [l,r], a_i =/= i for all i, and the number of pairs (i,j) where A_i + A_j = i + j, is maximised.
    
    

*/


int main () {
    ios_base::sync_with_stdio(false);
    
    int t;
    cin >> t;

    while (t--)
    {

    }

    cout << endl;
    return 0;
}